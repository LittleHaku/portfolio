---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = "", id } = Astro.props as Props
---

<section
  id={id}
  data-section={id}
  class={`section scroll-m-24 w-full mx-auto container lg:max-w-4xl md:max-w-2xl ${className}`}
>
  <slot />
</section>
